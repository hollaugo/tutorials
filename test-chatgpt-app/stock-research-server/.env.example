# Stock Research Server Configuration
# No API keys required - yfinance uses public Yahoo Finance data

# Transport: "stdio" (default) or "http" (Streamable HTTP)
MCP_TRANSPORT=stdio

# Port for HTTP transport (only used when MCP_TRANSPORT=http)
MCP_PORT=8000

# Optional: Set logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
